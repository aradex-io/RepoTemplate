# Architecture Overview

> Replace this with a real map of your system as it grows. Keep it current — an
> outdated architecture doc is worse than none.

## System map
A few sentences on what this project is and its major components.

## Components
| Component | Location | Responsibility |
|-----------|----------|----------------|
| _example_ | `src/...` | _what it does_ |

## Data flow
How requests/data move through the system (a diagram or a short list of steps).

## Key decisions
See `docs/adr/` for the *why* behind the structure here.

## Conventions
Project-specific patterns an agent can't infer from the code alone (naming,
error handling, layering rules, things the team has been burned by).
